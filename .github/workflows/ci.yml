name: CI exemplo

on:
  push:
   branches:
   - main

jobs:

  validar:
    runs-on: ubuntu-latest
    steps:
      - name: Validar código
        run: echo "Código validado"

  build:
    runs-on: ubuntu-latest
    needs: validar
    steps:
      - name: Simular build
        run: echo "Build realizado"
